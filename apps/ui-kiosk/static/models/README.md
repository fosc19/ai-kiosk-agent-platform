# Avatar models

Large VRM avatar files are intentionally not included in this portfolio repository.

To run the 3D avatar locally, place a compatible `.vrm` model in this folder and name it:

```text
avatar.vrm
```

You can create one with VRoid Studio or use a model with a compatible license.
